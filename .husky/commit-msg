#!/usr/bin/env sh
# Stellumio — commit-msg hook.
# Enforces Conventional Commits with Stellumio scopes via commitlint.
# The config lives in commitlint.config.mjs.

npx --no-install commitlint --edit "$1"
